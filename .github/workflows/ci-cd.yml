name: 🧪 Tests Simple

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: 🔍 Tester l'application
    runs-on: ubuntu-latest  
    
    steps:
    - name: 📥 Télécharger le code
      uses: actions/checkout@v4
      
    - name: ⚙️ Installer Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'  
        cache: 'npm'        
        
    - name: 📦 Installer les packages
      run: npm ci  
      
    - name: 🧪 Exécuter les tests
      run: npm test
      
    - name: 🔍 Vérifier le code (ESLint)
      run: npm run lint
      
    - name: ✅ Tests réussis !
      run: echo "🎉 Tous les tests ont passé !"